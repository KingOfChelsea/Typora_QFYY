# NIS_SQL配置

## 1.类word模版非元素锁定不能修改（参数配置）

```sql
 select rr.*,rr.rowid from nsr_dawn_params_param rr where rr.code ='RONRecord.WordRecordUnProtectTemplateId';
```

## 2.NIS左侧菜单挂出

```sql
select c.*,c.rowid from nis_plugin_conf c where c.name like '% %'
```

## 3.NIS患者工作卡片护理级别颜色配置

```sql
select * from NIS.NIS_IPR_NURSINGLEVEL t
```

## 4.NIS_XML配置

- 配置血糖单
- 配置住院护士站

```sql
select a.*,a.rowid from nsr_nurse_config a 
```

![image-20251218104721718](https://gitee.com/HavertzPlatform/worker-picgo/raw/master/image-20251218104721718.png)
