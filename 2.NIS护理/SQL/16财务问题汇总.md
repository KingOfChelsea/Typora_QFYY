# 财务问题汇总

## 1.住院订单头表索引唯一性报错（祈福）

- 问题**如图3**所示，跟SQL排查发现，SQL语句去更新`fin_ipb_order`的数据，因为索引的唯一性质（如**图1**所示），把其他的订单的ORDER_ID前面加入`B`,**如图2**所示，

```sql
update fin_ipb_order
   set invoice_no       = 'E2026062150599',
       balance_no       = '1',
       balance_oper_code = 'admin',
       balance_date     = to_date('2026-06-21 20:27:19', 'yyyy-MM-dd hh24:mi:ss'),
       begin_date       = case when begin_date > to_date('2000-01-01 00:00:00','yyyy-MM-dd hh24:mi:ss') then begin_date else to_Date('2026-06-20 16:26:00', 'yyyy-MM-dd hh24:mi:ss') end,
       end_date         = case when end_date > to_date('2000-01-01 00:00:00','yyyy-MM-dd hh24:mi:ss') then end_date else to_Date('2026-06-21 19:40:59', 'yyyy-MM-dd hh24:mi:ss') end,
       order_state      = '1'
 where order_id = '2026542120500024875'
   and inpatient_no = 'ZY001B100519003'
   and order_state = '0'

Error occurred: [1] (ORA-00001: 违反唯一约束条件 (HIS50.PK_FIN_IPB_ORDER)
)
```

---
![image-20260622160411557](https://gitee.com/HavertzPlatform/worker-picgo/raw/master/20260622160411601.png)

<center>图1</center>

---

![image-20260622160629363](https://gitee.com/HavertzPlatform/worker-picgo/raw/master/20260622160629389.png)

<center>图2</center>

---

![image-20260622160229537](https://gitee.com/HavertzPlatform/worker-picgo/raw/master/20260622160229580.png)

<center>图3</center>

## 2.医生申请退单无法在终端取消确认找到数据，需要手工改门诊费用表中的可退数量

- 如下图所示，医生发起了申请终端取消中没有对应数据，需要在费用表中的可退数量增加数量，确保数据正确

![image-20260622161234536](https://gitee.com/HavertzPlatform/worker-picgo/raw/master/20260622161234592.png)

<center>图3</center>

## 3.自制剂补数

<img src="https://gitee.com/HavertzPlatform/worker-picgo/raw/master/20260817103626207.png" alt="image-20260817103625996" style="zoom:50%;" />

```sql
-- 查询数据
select a.mo_order,a.own_cost,a.fee_date,a.qty,a.*,ROWID from fin_ipb_medicinelist a
/*where a.inpatient_no = 'ZY0010000502461'
and (select i.self_flag from pha_com_baseinfo i where i.drug_code = a.drug_code) = '1'*/
where a.mo_order like '%124584523%'; --处方号
select a.recipe_no,a.sequence_no,sum(a.own_cost),sum(round(a.qty*a.unit_price,2)) from fin_opb_feedetail_self a
where a.recipe_no like '%124584523%' --处方号
group by a.recipe_no,a.sequence_no ;

select a.unit_price,a.qty,a.qty*a.unit_price,round(a.qty*a.unit_price,2) roundp,a.own_cost,a.package_code,a.oper_date,a.*,rowid from fin_opb_feedetail_self a
where a.recipe_no like '%124584523%' --处方号;
```

```sql
--住院
--通过自制剂Y码+天数，查有无现成数据，有就把recipe_no放进下面insert语句的select里
select a.*, rowid
  from fin_opb_feedetail_self a
 -- where a.recipe_no = '2746576'
 where a.package_code = 'Y000006919'
   and a.patient_type = '2'
   and a.days = 2
 order by a.oper_date desc;
   
--insert
insert into fin_opb_feedetail_self 
(RECIPE_NO,SEQUENCE_NO,TRANS_TYPE,CLINIC_CODE,CARD_NO,ITEM_CODE,ITEM_NAME,SPECS,DOSE_MODEL_CODE,FEE_CODE,CLASS_CODE,UNIT_PRICE,QTY,DAYS,DOSE_ONCE,DOSE_UNIT,OWN_COST,EXEC_DPCD,EXEC_DPNM,COMB_NO,OPER_CODE,OPER_DATE,ECO_COST,EXT_FLAG3,PACKAGE_CODE,PACKAGE_NAME,MO_ORDER,OVER_COST,EXCESS_COST,DRUG_OWNCOST,COST_SOURCE,SUBJOB_FLAG,ACCOUNT_FLAG,UPDATE_SEQUENCENO,DOCTINDEPT,MEDICALGROUPCODE,ACCOUNT_NO,GROUP_CODE,BALANCENO,PATIENT_TYPE,SI_UPLOAD_FLAG)
with t1 as
 (select s.item_code,
         s.item_name,
         s.specs,
         s.dose_model_code,
         s.fee_code,
         s.class_code,
         s.unit_price,
         s.qty,
         s.days,
         s.dose_once,
         s.dose_unit,
         s.own_cost,
         s.ext_flag3,
         s.patient_type
    from fin_opb_feedetail_self s
   where s.recipe_no = 'B125325702')  --现成数据
select a.mo_order,
       a.sequence_no,
       a.trans_type,
       a.inpatient_no,
       (select i.card_no
          from fin_ipr_inmaininfo i
         where i.inpatient_no = a.inpatient_no),
       t1.item_code,
       t1.item_name,
       t1.specs,
       t1.dose_model_code,
       t1.fee_code,
       t1.class_code,
       t1.unit_price,
       t1.qty,
       t1.days,
       t1.dose_once,
       t1.dose_unit,
       t1.own_cost,
       a.execute_deptcode,
       fun_get_deptname(a.execute_deptcode),
       (select o.comb_no
          from met_ipm_order o
         where o.mo_order = a.mo_order
           and rownum = 1),
       a.fee_opercode,
       --fun_get_employeename(a.fee_opercode),
       a.fee_date,
       0.00,
       0,
       a.drug_code,
       a.drug_name,
       (select o.mark6
          from met_ipm_order o
         where o.mo_order = a.mo_order
           and rownum = 1),
       '',
       '',
       '',
       '',
       '',
       '',
       '',
       '',
       '',
       '',
       '',
       '',
       '2', --门诊1 住院2
       nvl((select a.value
             from fin_com_undruginfo_extend a
            where a.undrug_code = b.drug_code
              and a.type = '997'
              and rownum = 1),
           0) --是否不上传医保
  from fin_ipb_medicinelist a
  left join PHA_COM_CRAMFORMULAINFO b
    on a.drug_code = b.package_code
  left join pha_com_baseinfo s
    on s.drug_code = b.drug_code
  left join fin_com_undruginfo u
    on u.item_code = b.drug_code
  left join t1
    on t1.item_code = b.drug_code
 where a.mo_order = '124584523'  --待补数据的mo_order
   and a.sequence_no = '1'

```

